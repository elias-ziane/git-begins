Cloner un repository = git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY

les 3 commandes de push : git add
			  git commit -m "votre message"
			  git push

Commande permettant de pull : git pull


Commande permettant de créer une branche : git branch "Nom de la branche"

Commande permettant de switcher d'un branche a une autre : git checkout "nom de la branche"
